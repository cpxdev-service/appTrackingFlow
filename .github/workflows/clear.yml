name: Delete old workflow runs
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  delete_runs:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: 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
          repository: cpxdev-service/appTrackingFlow
          retain_days: 1
          keep_minimum_runs: 6
